# 2026-09-23 项目地图重建历程草稿

整理者：Codex。来源范围：本次用户“提交推送，根据现有代码和新的项目地图 skill，重构 maintenance 的项目地图，旧的删掉”的请求，以及本次对当前仓库源码、旧地图和历史变更报告的核对。未绑定宿主公开事件索引，因此这里只保存过程草稿，不冒充可展开的正式 history 记录。

旧 `docs/project` 有 157 个已跟踪文件，入口混合多个旧安装时点、未验收状态和后来已改变的设计。决定沿用原项目 ID，保留代码与原报告，在新地图中只维护当前职责、三个关键合同、当前实现、已知耦合缺口及独立验证记录。旧地图从当前工作树删除，历史可在 `d3fdbe3:docs/project/` 查回。

首次尝试用递归删除旧目录被执行环境拒绝；改为先核验仓库路径，再对 Git 列出的明确文件逐项删除。该纠正只改变地图文件的删除方式，不触碰产品代码、Launcher 或真实实例。新地图校验为 19 条记录、零错误和零警告；绑定源码扫描 1015 个文件，受限检索与记录读取可用，文档阅读页导出成功。未运行产品测试或真实实例验收。提交及远端推送结果以本次任务最终回执为准。
