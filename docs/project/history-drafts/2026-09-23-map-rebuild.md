# 2026-09-23 项目地图重建历程草稿

整理者：Codex。来源为用户要求“提交推送，根据现有代码和新的项目地图 skill，重构 maintenance 的项目地图，旧的删掉”，以及本次对源码、旧地图、历史规格和 HTML 阅读页的核对。未绑定宿主公开事件索引，因此这里只保存可追溯过程草稿，不冒充正式会话 history。

旧 `docs/project` 混合多个安装时点、后来变更的范围语义和历史验收。初次将地图收缩成 19 条当前记录后，新版 skill 的完整建图检查指出缺少功能路径与模块源码图。处理时先把初版和旧版清单保存到 `migration/coverage-before-20260923.json`，再逐项核对 103 条旧资产。有效需求、对象与接口进入重写后的 62 条记录；唯一明确撤销的“实例新建工作区自动登记”指向新范围决定，时点实施/验证在 Git 与原报告保留。`migration/disposition-20260923.json`列出每条去向。

工作中发现 `composition-root.ts` 仍直接装配 DSH、Launcher、Lynn、GPT，因此在当前问题记录中保持“全局解耦未完成”的判断。用户已纠正“通用图模型”：新对象和图注只把 Lynn 图与引用视为插件数据，由对应 adapter 映射。重新引入学习交接、Vault、阅读器、运行恢复、扩展页等仍有效的要求时，把历史验收与今日实现分开。

扩展 Archify 整体图、新建五条主要路径的流程图，生成当前 GitNexus 生产源码快照。首次整体图新增连接存在节点穿越和标签遮挡，调整连线后原生校验通过；仍保留可放大查看的全图文字和单个连线交叉提醒。结构校验、完整交付机械检查通过；在 HTML 中实际检查了整体图到 `MOD-endpoint`、流程图到 `IF-host-writeback`，以及源码图文件、函数、调用链和来源限制。产品代码、Launcher、真实实例均未修改或重新验收。提交和推送结果以本任务最后的 Git 回执为准。
