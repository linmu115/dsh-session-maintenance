# 2026-09-22 Lynn 与独立 GPT adapter 开发历程草稿

来源：本任务用户当前指示及其指定的任务 01a0c759-8c96-7460-bcbd-d4bf40512308。整理者：Codex。本文件只记录此次实际开发，未绑定宿主事件索引，不宣称正式可展开历程。

用户将目标进一步明确为“数据抽象映射回去后功能保持”，并拒绝在 Maintenance 建通用图模型。实施中发现日志之外还存在 Core 聚合、sessionExtensionData 和 StickerLocalStore，因此新增不透明附属记录和 Lynn 存储映射。将图、引用和业务目录规则从核心迁往 adapter。

真实源码组件回读核验发现 DAG 工厂返回 graph 子对象，修正验收调用后通过；GPT 过滤事件后的坐标转换原本没有覆盖 checkpoint/operation，已由 GPT 自己的转换器补齐并验证。schema 29 增加了附属数据版本，保留扫描显式支持新模式；初轮回归因旧模式白名单拒绝而失败，修正后保留原有破坏性操作阻断条件。旧目录断言同步为一个 Lynn 入口。

指定任务已把测试实例重新启动；本轮已请求用户正常停止，当前不替换运行实例文件。Engine 启停入口原先依赖已移除的 Launcher hook，改为独立安装描述文件。安装及最终结果继续记录在本轮变更报告。

最终回归 88 文件、416 项通过，发行包 58 项哈希通过。旧 Engine 正常退出并核实 drain / owner release，备份后独立切换到 .71，Lynn 与 GPT 两个包加载通过，旧会话与配置摘要一致。真实测试实例未停机，.45 接入组件留待正常停止后安装。业务目录初次查询误用逻辑 profile，返回 EXTENSION_NOT_FOUND；按照现有业务登记的物理 profile web 查询成功，原登记没有被改写。
