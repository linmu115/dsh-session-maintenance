# 本轮开发历程草稿（未索引）

来源：当前用户公开任务「按文档对各个插件进行架构升级」及本轮工具验证。未绑定可展开的宿主事件索引，不伪造 history-event 回执。

按九份已确认文档实施，保留用户要求暂缓的 DAG adapter、GPT Compat 和多实例绑定范围。先建立本地能力和通用接入口，再完成受管门禁、安装检查、候选打包及说明。

回归中识别了旧测试的镜像自动开启/Engine 自动启动假设、版本清单断言和打包缺少共享文件问题。修订生产默认行为后，相应旧测试按新的公开合同复验，历史失败保留在交付报告。恢复测试还发现旧运行读取后来更新的共享目录导致条目丢失，改为保留该运行自己的固定目录。

ThoughtDAG 的 pnpm 自动依赖检查曾触发异种包管理器安装；随后停止该流程，用原 npm lock 和 legacy peer 模式恢复依赖，直接调用构建脚本，未引入新的依赖版本清单。

结果与具体测试数字以交付报告为准；没有安装真实实例或更改实际会话/Vault。原有未提交改动保持，不将它们当作本轮首次产出。


## DAG 混合环境阻断与用户纠正

来源：当前用户注释确认 DAG 仅依赖 Core，并指出已同步图应从 Maintenance 真源恢复。旧路径因 Maintenance 服务存在而抢占本地接口；已补充公共端口与 adapter 恢复测试。本轮新增：thoughtdag 可选 adapter 将真源中的旧主干转换到统一会话数据，支持新格式往返并保留原件。部署停在 .35 未被接入检测白名单识别，未启动 DSH，未通过真实页面验收。详见 docs/changes/2026-09-20-dag-session-data.md。 当前来源尚未单独绑定事件索引，本段是过程草稿。

## 兼容白名单后续修正（来源：本任务用户确认，待绑定事件索引）

此前 .35 发布遗漏两处兼容名单，安装验证通过但注册失败。用户要求解释后确认修复并继续流程。本轮选择独立协议声明与同一检查入口，冻结旧版本兼容而不无限放宽 semver；部署前增加实际发行包注册验证。结果：42 项测试通过，新发行包真实注册、启动、图保存刷新及正常停止通过。安装两侧 Bridge 后，check 不更新原注册指纹，教程的升级复核流程不完整；按用户要求停在 Bridge 启动验收，尚未进行网页绑定和贴纸安装。

## 暂缓 Maintenance，转独立验收（本任务用户最新指令，待绑定来源索引）

用户纠正普通插件变化不应阻断，未知会话类型应原样保存；随后要求先登记、交接，暂缓实现并关闭引擎验收其他插件。新增 docs/issues/2026-09-20-managed-plugin-composition.md 与 docs/handoffs/2026-09-20-maintenance-and-independent-acceptance.md，明确替代此前每次人工复核的建议。

独立验收结果：正式解除注册，移除接入插件并关闭引擎；Bridge .5 UI 已按用户反馈修复并验收浅/深色与断开重连。Sticker .6 独立启动等待 obsidianBridgeLifecycle 失败，已撤回该包恢复基线，停止继续处理。Core/DAG 新本地业务尚未完整验收。
