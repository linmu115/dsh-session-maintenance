# 旧地图记录定位

2026-09-23 重建前的完整地图位于 Git 修订 `d3fdbe3:docs/project/`。其中 16 条 `HIST-*` 历程和 1 条 Lynn/GPT 日志已恢复到当前“开发日志”，见 [[JRN-20260923-history-restoration]]。以下仅为其他常用旧入口到当前责任记录的阅读指针，不声称新旧正文或能力完全等价。旧记录的当时版本、来源和验证应从该修订原文核对。

| 旧记录 ID | 当前阅读入口 |
| --- | --- |
| `MOD-canonical`、`OBJ-session`、`OBJ-version` | [[MOD-core]] |
| `MOD-runtime`、`IF-runtime` | [[MOD-core]]、[[MOD-endpoint]] |
| `MOD-host`、`MOD-harness-dsh` | [[MOD-dsh-host]]、[[IF-host-writeback]] |
| `MOD-business`、`IF-extension` | [[MOD-plugin-adapters]]、[[IF-plugin-data]] |
| `MOD-dashboard`、`MOD-reader` | [[MOD-ui]] |
| `IMP-current` | [[IMP-current-source]] |
| `REQ-detached-instance-attach-sync` | [[REQ-sync-coverage]]、[[IF-endpoint-sync]] |
| `IMP-lynn-adapter` | [[MOD-plugin-adapters]]、[[IF-plugin-data]] |

其他旧 ID 仍可用 `git show d3fdbe3:docs/project/project.yaml` 或该修订的 `records/` 查回。重建地图没有退役对应产品功能，也没有改写历史规格及验收报告。
