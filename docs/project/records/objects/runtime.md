---
id: OBJ-runtime
kind: object
title: 原生空间、运行租约和恢复证据
status: current
summary: 一次运行固定来源和目标空间，租约、日志、检查点及宿主回执共同约束恢复。
---
实例健康、进程是否存在与会话写入权是不同事实。准备、运行、排空和恢复要按身份与回执串起来；未知退出状态不能凭端口不通自动回收。恢复时核对旧投放、未完成写入及宿主所有权，不能以删除锁文件代替正式关闭。实现 [[MOD-runtime]] 和宿主合同 [[IF-host-writeback]]。
