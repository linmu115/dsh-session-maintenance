# Vault 绑定列表与弹窗排布

用户反馈实例列表和弹窗留白过大、布局不好看。Dashboard 0.1.8 保留原绑定流程，调整信息层级和密度，复用现有工作台颜色、字体、图标与焦点样式。

- 实例页用单一列表呈现，实例名、配置和管理入口固定对齐，移除外层重复卡片。实例名可换行；窄屏隐藏装饰图标与重复操作文字。
- 弹窗宽度收至 640px，标题与配置并列，图标关闭按钮置右；已绑定数量、刷新、新建集中到列表工具栏。
- 每个 Vault 用名称、状态点、路径与解绑按钮组成一行；只有离线或需检查时显示补充说明。解释文字置底，长列表独立滚动。
- 保持打开弹窗、Esc 关闭、忙碌时禁止关闭、绑定/解绑的操作ID和重试逻辑；没有改后端、存储或绑定合同。

类型检查及原有 4 项相关交互回归通过。内置浏览器合成页已检查实例列表与弹窗视觉；实际部署和验证结果补入同任务历程。部署只覆盖 Dashboard 静态资源，保留 Engine .45 与现有 Vault 状态，原发行包 BUILD-INFO 保持归档事实，前端覆盖版本另留安装回执。

实际安装：静态资源覆盖到当前Engine .45发行目录，17个HTTP资源哈希与构建一致，原Dashboard已备份。引擎PID不变，接入记录、同步策略和Vault data.json哈希未变。内置浏览器真实页面已检查实例列表、弹窗、刷新、关闭和浅深色主题；实际窗口宽度约516px时布局可用。未执行真实绑定/解绑或文件夹选择。本机回执位于 artifacts/vault-binding-layout-20260919/installed.json，发行目录内DASHBOARD-OVERLAY.json记录独立前端版本，不改写原归档BUILD-INFO。
