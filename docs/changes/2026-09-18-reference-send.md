# 引用预检不应派生会话

修复镜像会话在选择模型或执行内部历史压缩时创建同名派生会话的问题。模型选择和四类 context 准备事件暂缓提交，首条真实内容到来后带上完整前序记录；恢复判断使用同样规则。已有派生会话未删除。

更新 RC2 插件、引擎及 GPT 格式兼容版本。projection-runtime 与 migration-recovery 两份测试共 26 项通过，插件及引擎类型检查通过。实例已部署，真实引用提交仍待用户验证。
