# Lynn 修复安装与人工验收交接

2026-09-22：用户确认正常停止测试实例，并要求后续不使用 Computer Use，由用户亲自操作界面验收。

## 当前安装状态

- 测试实例：`i-27c4d5a7-bdb5-4b8a-8d95-6267f47499c5`（0.1.5-rc.2 测试），物理 profile `web`，Maintenance 身份 `web-i27c4`。
- Engine **0.1.43-rc2.74** 已独立启动；实例接入组件 **0.2.27-rc2.47** 已安装；Lynn adapter 与独立 gpt-compat adapter 均已加载，适配目录无错误。
- 正式目录接入 `connected`、issues 为空。这证明安装和目录接入通过，不代表实例运行期握手和同步往返已验收。
- 测试实例在安装前后均核验为 `stopped`，由用户启动。未使用 Computer Use。
- Engine 旧进程正常 SIGINT 退出，`shutdown.drained`、`owner.released`、`shutdown.completed` 和停止回执齐全。旧 PID 后来被无关浏览器进程复用，通过进程路径和创建时间区分，没有操作该进程。

## 后端验证

- 58 个发行文件、19 个已安装插件文件与发行哈希一致。
- 已安装插件在真实依赖下加载成功；实际宿主持久化组件在隔离数据上通过创建、追加、flush、关闭、重新读取和只读拒写验证。
- 重新生成两个接入回执，23 个运行构件哈希通过。
- 其他 16 个插件的 984 个文件未变；共 1,165 个应保留文件校验一致。profile package.json 只有 Maintenance 依赖指向更新。
- 553 个逻辑会话、25 个派生关系、553 个工作区成员、496 个删除标记、117 个扩展对象逐行内容与本次备份一致。
- Vault 绑定、同步范围及 testvault Companion 设置与本次备份一致。
- 本轮只安装前一提交 `06a4d6c` 的已验证修复，没有新增业务代码。其 20 项相关测试、类型检查和隔离组件功能往返证据见前一验收报告。

## 用户操作验收清单

所有写入测试使用专用验收会话/笔记。既有测试笔记为 `D:/obsidian/testvault/Lynn验收-20260922.md`；既有两条测试会话 ID 见 testvault 验收报告。

1. **启动与连接**：通过 Launcher 启动「0.1.5-rc.2 测试」，在 testvault 内置浏览器打开本次实例的 DSH WebUI。检查接入状态、会话列表及发送一条短消息是否正常。不要复用旧端口作为身份依据。
2. **Lynn 功能回归**：打开既有测试会话，检查思维图、会话贴纸和引用详情；从测试笔记选择文段引用到 DSH，提交后检查模型能读取；从引用跳回笔记确认定位与回链。普通贴纸也需新增/编辑一次并刷新后确认保留。
3. **同步前提**：在 Maintenance 的实例同步范围中确认专用验收工作区确实属于该测试实例的已生效范围。目录显示 connected 本身不代表有已生效映射；当前安装没有扩大或改动用户原范围。如果范围未配置或显示下次启动生效，先完成配置和正常启动，再做第 4、5 项。
4. **运行期变化**：在验收工作区新建测试会话，依次追加消息、改名、移动到另一个已纳入验收范围的工作区、归档、取消归档；每步检查 Maintenance 中内容、名称、归属和状态对应更新，无重复会话。删除测试会话后检查删除状态；不要用正式历史会话测试删除。
5. **映射后的功能保持**：使用产品已有的映射/派生入口，把带有引用、普通贴纸和思维图的测试会话恢复到验收目标，再打开目标插件检查内容可读、引用定位正确、可继续编辑并保存，源会话未被覆盖。若引用依赖的源会话未纳入映射，应有明确拒绝或不可用提示，不可错误跳到别的会话。缺插件保留但不投放、未知数据保留且折叠仍需专门目标条件，不能仅凭当前完整组合判定通过。
6. **独立 GPT adapter**：使用 GPT 兼容入口创建专用测试会话并续写；映射/派生后检查历史可读、继续回复正常。涉及工具结果或检查点时，检查引用归属正确，无缺失或串线。不要为此修改正式账号或已有生产会话。

用户可先完成第 1、2 项并反馈哪一步异常；运行期捕获完整性、写入屏障与映射回执由后续后端检查交叉确认。截图或“看起来正常”不足以独立证明排他写入和数据恢复完整性。整套同步尚未标记验收通过。

## 回滚和证据位置

- 本次备份及完整验证：`D:/AI/DeepSeekHarness-Plugin/artifacts/lynn-adapters-20260922/instance-install-rc2.47-20260922`，含 `verification.json`、`engine-stop-receipt.json`、旧配置、插件、会话与维护数据库备份、新接入回执。
- 当前发行路径：`D:/AI/DeepSeekHarness-Plugin/artifacts/lynn-adapters-20260922/engine-0.1.43-rc2.74-candidate`。目录名保留构建时的 candidate，实际安装状态以本报告及安装回执为准。
- 旧实例插件目录保留为 `dsh-session-maintenance.backup-rc2.46-20260922`，旧引擎发行包及安装指向有备份。回滚同样需要正常停止实例/Engine 后实施。
- Launcher 自带窗口先前的 HTTP 431 未由本轮修复；本轮没有界面操作，安装后 UI 验收交由用户。
