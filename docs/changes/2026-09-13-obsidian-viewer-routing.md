# Obsidian 接收页面隔离与 RC2 配套发布

Annotation 上游扩展的版本清单遗漏 rc2.2，导致已启用的跨会话目录被当作不可用。补齐 0.3.12-rc2.1、rc2.2、rc2.3，并分别报告未配置、停用、版本不兼容与 Adapter 缺失。同步 Engine 0.1.33-rc2.5 与插件 0.2.26-rc2.4 的启动校验清单，保留旧发布的兼容性。

验证：真实 Engine + 认证 HTTP 场景覆盖三版目录读取、未知版本拒绝、停用提示及固定上游捕获/读取；37 项运行绑定、集成和扩展数据测试通过；工作区构建通过。测试全部使用合成目录，未写入真实会话。
