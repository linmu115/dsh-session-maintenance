# 原生 Agent 上下文管理需求确认

2026-09-15，用户确认模型可管理自身主干及上下文窗口，并新增按用户请求索引会话轮次的结构。用户明确要求本阶段暂不处理托管引擎。

本次新增[原生 Agent 补充规格](../superpowers/specs/2026-09-15-native-agent-context-management.md)，同步[总需求](../superpowers/specs/2026-09-10-session-context-graph-requirements.md) D17–D22 和[插件改动清单](../superpowers/specs/2026-09-10-session-context-graph-plugin-changes.md) W19–W24。

确认内容包括：真实用户请求结构及固定范围索引；授权/活动窗口/保留集合三层状态；不连续片段；默认释放材料；独立暂停/恢复及永久解除；来源状态、覆盖、保留和定位；按允许范围发现来源；DSH 原生工具注册与当前会话写入作用域；Maintenance Adapter 同步。

规格明确真正释放须改变后续原生模型输入且保留原始事件；读取预算清空、隐藏气泡和删除图日志均不能冒充释放。请求列表和日志保持有界，不新增全文快照或自动摘要备份。已有主干/引用发布保持其原交付状态，新增阶段明确待实现。

文档验证：检查相对 Markdown 文件链接、需求和任务编号、验收引用、UTF-8 与 Git whitespace 差异；范围只含四份 Markdown 文档。NCA01–NCA24 为待实施验收条件，本次不运行无关代码构建或模型测试，不改运行副本及真实会话数据。

检查结果：四份文档、18 个相对文件链接均有效，NC01–NC08、NCA01–NCA24、D01–D22 和 W01–W24 编号连续且无重复，UTF-8、代码围栏及 `git diff --check` 通过。独立只读审查未发现确认需求遗漏、固定范围冲突、初始问答降级或托管范围混入。
