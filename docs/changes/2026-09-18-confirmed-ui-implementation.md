# 同步名单编辑模式与固定操作栏（2026-09-18）

按已确认方案实现，仅源码和本地 Dashboard 构建，未部署到运行 Engine。

Codex 项目与 Maintenance 工作区名单默认浏览，点击编辑后才允许选择并显示保存/撤销。保存成功退出编辑，失败保留编辑态；目录刷新保留勾选和编辑基线。Codex 外部版本冲突保留草稿并提示撤销后重新读取；Maintenance 冲突读取最新配置但保留草稿供核对。两个栏目保持挂载，互不清除草稿。

名单独立滚动，操作栏根据页面真实上边界和窗口高度始终保留在视口内；窄屏按钮可换行。技术状态和当前/已保存范围收进运行详情，减少常驻小字，仍保留未选项目影响和错误说明。

验证：Dashboard typecheck/build 通过。完整 Dashboard 24 文件/81 项通过，最后布局及冲突调整后受影响 3 文件/15 项重跑通过。真实 React 组件使用合成 API 的浏览器检查涵盖 18 项长名单、编辑入口、宽/窄屏底栏；1280x720 底栏下缘约700px；390x780 窄屏仍可见。页面无 error 日志。

验证入口：apps/dashboard/test/fixtures/sync-ui.html。仅合成数据，不写真实 Engine 配置。

运行身份只读核验：RC2 i-7ecb6c19-80a5-4c2e-97e6-484bbfc0e926 / web 在本次检查为 running，维护 run 同为 running。原实例安装保持不变，未执行停止/重启，未将源码构建当作安装成功。
