# Engine .45 / Dashboard .1.7 配套交付

用户要求推送已完成改动、更新项目地图并替换当前安装版。Engine 升至 0.1.33-rc2.45，Dashboard 升至 0.1.7，包含真源名单折叠选择、扩展导航加载修复及独立 Vault 绑定管理；Obsidian 侧配套 0.7.0-rc2.4。Engine 运行证明白名单同步接纳 .45，保留既有版本兼容。

发布前 Maintenance 55 项相关测试通过，含真实发行版本的运行证明与接入测试；Engine 类型检查及 Dashboard 构建通过。Obsidian 侧 21 项相关测试、类型检查和构建通过。

DSH 侧 CLI 功能已在独立 dsh-obsidian-bridge 仓库提交并推送，main 为 d3b21f7。Obsidian 侧当前绑定修改 d162f5b 已推送。两项属于不同项目，无需重复提交 CLI 或回写已退役 Suite。

本提交为发行准备。实际激活、备份及只读检查结果另补入项目地图；这里不提前声称生产安装完成。真实绑定及笔记不作测试写入。

发行检查发现共享契约新增使配套 DSH 插件 bundle 字节发生变化，不能复用 .30 归档。配套归档独立升为 .31 并通过正常发布校验；现有已支持的 .29 插件无需为 Engine 升级替换，实际安装范围据回执记录。
