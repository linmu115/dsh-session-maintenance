# DSH 设置只保留完整看板入口

用户要求精简 DSH 设置页注册的 Session Maintenance 面板。现仅保留简短说明和“打开完整看板”按钮；移除历史登记、扫描范围、备份保留、批量应用表单，以及保存、扫描、同步、重读按钮。页面挂载不再调用 settings:get，打开完整看板仍使用全局入口，不依赖当前会话映射。清理对应旧表单样式，保留打开过程反馈与错误提示。

完整看板、后端设置与同步接口、会话右键入口均保持原有职责；DSH 宿主自己的“打开配置文件”等通用控件不属于此注册面板。

插件源版本 0.2.26-rc2.32；类型检查、设置注册与全局看板入口7项测试、插件构建通过。合成页面在内置浏览器确认只有一段说明和一个按钮；安装版 DSH 设置页视觉未验收（外部浏览器句柄不可用）。

实际安装只覆盖原 0.2.26-rc2.29 插件的 lib/client/index.js，覆盖前已备份并核验构件回执。后端、package.json、profile配置、同步策略及回执保持哈希一致；不冒充完整升级到 .32。DSH自带HMR检测新文件，认证HTTP取得当前启动图对应的新脚本，确认包含新版入口；bootId与origin不变，无需停止实例。证据：D:/AI/DeepSeekHarness-Plugin/artifacts/settings-entry-20260919/installed.json。
