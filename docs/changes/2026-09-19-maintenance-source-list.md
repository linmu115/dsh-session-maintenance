# Maintenance 真源名单显示改进 · 2026-09-19

来源：当前用户请求及截图。保留实例选择，将同步范围改为默认折叠的 Maintenance 真源名单，使用 Codex 名单风格的搜索、计数、勾选和可折叠详情。折叠状态显示简短已保存范围及未保存/待生效状态。

沿用原全部/显式名单语义及实例策略 API；在全部模式取消工作区会转为保留其他选择的显式名单。搜索不改变勾选。保存/撤销、409 冲突处理、实例切换时旧请求隔离仍保持原行为。

验证：instance-workspace-page、sync-sections、codex-project-mapping 共 16 项测试通过；Dashboard 类型检查和构建通过。合成浏览器完成默认折叠、展开、搜索、勾选、收起重开保留草稿与保存提示验收，观察并修正低窗口高度下模式控件过高问题。

部署边界：没有替换当前运行安装；真实实例 UI 未验收。未修改真实名单、用户数据或运行生命周期。项目地图新增 REQ-maintenance-source-list 与 HIST-maintenance-source-list，未新增更新记录。
