# 保存回执、对齐进度与新会话发现

用户要求修复范围保存等待、对齐失败阻断新会话导入，保留“编辑 → 勾选 → 保存 / 撤销”。Maintenance 主体只调用通用同步协议；原生路径、投影身份、宿主锁和压缩传输在 DSH adapter 内。

## 已安装和在线核验

截至 2026-09-22 21:10，实际运行 Engine **0.1.43-rc2.80** / Dashboard **0.1.17** / DSH 接入 **0.2.27-rc2.48**。测试实例稳定 ID `i-27c4d5a7-bdb5-4b8a-8d95-6267f47499c5`，实际 profile 为 web，Maintenance 身份为 web-i27c4。Engine ready，目录 connected；DSH 当前 boot `6bf5161b-9545-4a28-b759-830d89e4121e`，属于独立目录接入，没有托管 run。正式 Start 包装器曾因没有托管 run 返回 SM_START_UNAVAILABLE，但实例实际已经启动，已核验身份，未重复启动。

- 在线保存相同的三个工作区，**16 ms** 返回。仅策略修订从 7 变为 8，选择名单和 includeUnassigned=false 不变。保存不再等待完整宿主对齐。
- 新会话导入不再依赖反向对齐先成功。“计算机四大”测试会话 `session-1596eacd-f815-4c3d-85df-895b8f17c2ac` 在 blocked 状态成功进入真源，逻辑 ID `logical-dsh-419ab0a5508b67323f12535d5a3556a2`，归属 `workspace_af28823bbf61b6cf46eaee0a`。
- 静态看板实际使用的 `/v1/canonical/projects` 目录接口已经包含该会话，详情接口返回 **29 条事件**。旧 `/v1/sessions` 是另一套历史目录接口，不能据其筛选结果判断当前静态看板。
- 页面仍保留编辑按钮，独立显示保存结果与后台对齐进度，轮询不覆盖编辑草稿。真实 UI 未验收；本轮没有使用 Computer Use。

## 修复内容与身份回退

1. 对齐作为可合并的后台任务执行；网络等待不占用 canonical 全局写入协调。生成快照及最终身份提交仍受协调，回执后重新检查范围修订和真源版本。正常关闭会等待任务收尾。
2. 新增 discover 命令，非 active 阶段仅导入新身份；已有身份返回 already-present，不以发现请求覆盖已有历史、归档或删除状态。保留 epoch、范围、完整落盘、插件捕获和版本检查。
3. 未完成首次对齐的工作区也使用 adapter 的相同目录规划规则读取原生数据，要求物理目录已被宿主登记；不再依赖只有成功对齐后才落盘的映射文件，也不按同名目录猜测归属。
4. 宿主对完整一致的会话直接确认；需要写入的会话才取得屏障。先 flush/drain，再保护插件写入，保护持续到刷新及释放结束。drain 前失败不隔离插件。
5. .79 现场自动导入暴露 1 条旧投影缺失 platform binding，导致重复导入。已正常停止 Maintenance，引擎退出、drain 和 owner release 回执通过；备份 post-import DB，确认原 554 条会话记录未改变后，将本轮 28 条自动导入整体回退至安装前备份。没有恢复、删除或覆盖原生会话文件。
6. .80 在 adapter 中识别 `dsh-maintenance_` 投影身份，再由装配层查询原有逻辑身份与别名；未知/损坏投影身份拒绝当作新会话导入。实际旧投影返回原 logical ID / already-present。重新导入后，原 554 条真源会话记录仍逐行一致。
7. 原生会话文件前后均 67 个，没有增删。两个测试会话事件分别从 28→29、20→21，原有事件前缀均完整保留。安装时其他插件、设置和数据共 1,163 个文件的哈希保持一致。

## 新发现的传输上限与待安装候选

较小范围的实际对齐已取得明确拒绝原因 DSH_BUSY：目标会话需要修改但宿主仍占用，安全屏障保留原文件，未强制释放。新会话陆续导入后，后续全量对齐变为 **HTTP 413**。只读测量当前选择中的 58 条投影快照 JSON 为 125,249,442 字节，超出旧宿主 64 MiB 接收限制。

候选 **Engine 0.1.43-rc2.81 / 接入组件 0.2.27-rc2.49** 已构建，尚未安装：

- DSH adapter 对超过 1 MiB 的请求异步 gzip，保留完整快照及跨会话/插件映射上下文，不拆掉引用关系。
- 宿主先认证，再接收最多 64 MiB 压缩数据；解压在 JSON 解析前限制为 256 MiB。损坏压缩、未知编码和解压超限分别拒绝；小请求继续兼容原 JSON。
- 当前快照压缩后 26,729,817 字节，满足两侧限制。实际宿主接收与完整对齐仍待安装核验，不能用压缩比例或单测代替。
- 打包脚本固定 ESM createRequire banner，使已有实际宿主依赖的加载支持可重复构建。

已请求用户再次通过 Launcher 正常停止测试实例，随后才能替换运行中的接入组件。没有安装 .81 到仍为 .48 的宿主组合，也没有强杀、直接 runtime shutdown 或更改 Launcher hook。

## 验证和证据

相关回归覆盖慢宿主期间保存返回、首次发现、已有身份拒写、epoch/范围/真源过期、编辑草稿保留、真实官方持久化锁、活跃会话无改写确认、drain 前缀保护、插件屏障恢复和 HTTP 拒绝阶段。新增压缩与宿主相关 11 项测试通过；最终候选打包的 41 项接入/构件测试通过；全仓 31 包类型检查通过；另 6 项发行嵌入校验测试通过。已安装 .80 的 41 个发行文件哈希及真实依赖加载/隔离持久化探针通过；.81/.49 候选实际依赖加载通过。

发行与证据根：`D:/AI/DeepSeekHarness-Plugin/artifacts/lynn-adapters-20260922`。

- 当前安装：`engine-0.1.43-rc2.80-final`。
- 待安装：`engine-0.1.43-rc2.81-candidate`。
- `sync-unblock-20260922/rollback-receipt.json`：本轮导入回退及哈希。
- `engine80-discovery-receipts.json`：blocked 状态新会话导入、旧投影防重。
- `engine80-save-and-static-page.json`：在线保存耗时、原选择保持、静态目录与详情。
- `engine80-native-preservation.json`：原生文件及两个测试会话前缀保持。
- `final-verification.json`：.80/.48 实际状态及当时 HTTP 413；新增数量是检查时点快照，后台发现仍在运行。
- `compression-live-size.json`：只读测量压缩大小，无会话正文。

用户 UI 验收项：编辑并保存范围，确认立即出现保存成功和独立同步进度；刷新静态看板，进入“计算机四大”查看测试会话。已有会话续写、归档/取消归档、删除的完整往返，以及新压缩协议的真实对齐，尚未标记通过。
