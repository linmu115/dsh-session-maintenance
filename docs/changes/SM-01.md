# SM-01 当前架构、支持与发布文档

日期：2026-09-05。基线：`0333e18`；承接 `4b49927` 与候选看板修复 `a6b4053`。

## 变更

- README 改为当前 Canonical 架构，说明 Engine、WebUI、DSH 插件、Adapter、Maintenance Provider 与 Launcher Hook 责任。
- 区分 Codex 只读导入、prepare 的标题同步和 Canonical 投影准备，以及运行期间插件直接回写。
- 支持矩阵补齐 RC1 精确版本约束；区分三类 Adapter 实现、合成验证、历史候选和已记录运行组合。
- 新增 Hook 接入文档，校对 schema-v1 四阶段字段和 CLI；明确 Provider 留在本仓库、通用 Hook 属于 Launcher，配置不能补足宿主能力。
- 安装/升级/恢复/卸载说明更新为当前投影和恢复边界；保留旧 RC2 Gateway 的适用条件，不恢复旧同步运行链。
- Generation 文档明确现有脚本的 Alpha2/RC2 打包范围，不冒充 RC1 当前组合发布。
- 集成复核补充 CLI 状态目录优先级；升级时区分活动写入与已隔离的历史现场，要求保护恢复点并确认格式兼容，不把所有历史隔离记录视为永久升级阻塞。

## 验证

- 对照 `apps/engine/src/cli.ts`、`external-lifecycle-provider.ts`、`composition-root.ts`、`dashboard-root.ts`、各 Adapter 的 COMPATIBILITY 文档及两类打包脚本核对命令、参数与责任。
- 对所有本次修改 Markdown 的本地链接做存在性检查；`git diff --check` 检查通过。
- 纯文档变更按 SM-01 要求不新增业务测试，不运行真实状态命令或部署流程。

## 状态与限制

已记录 live 基线仍是 Engine 0.1.14 / Maintenance 0.2.16 / SCM 0.3.1、`4b49927`。`a6b4053` 看板补丁仅标候选，实际切换与浏览器验收由发布步骤另行记录。本提交没有改运行代码、版本、锁文件、批准计划或审查存档；没有触碰其他工作树、真实 homes 或已安装 profile，没有推送或部署。
