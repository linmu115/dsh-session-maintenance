# 会话名称选择器兼容组合

日期：2026-09-15。

本次配套 Annotation Core 0.3.12-rc2.12：跨会话引用选择器复用 DSH 会话栏的可读标题，使用户能够按名称识别目标会话。ID 仍用于准确定位，工作区、分页、引用固定版本和截止位置的语义不变。

本仓库同步包版本和明确的 peer 兼容范围；Suite 同步成员清单。没有增加 Vault 扫描、后台轮询或托管引擎适配。

验证使用现有兼容清单、包契约和功能回归测试；部署结果由本批副本验收记录单独提供，源码版本不表示已发布公共安装包。

Maintenance 本次为 Engine 0.1.33-rc2.21 / 插件 0.2.26-rc2.17。明确接受 Core 0.3.12-rc2.12 的三个命名空间，以及 Sticker 0.7.3-rc2.18；继续保留全部六个扩展命名空间。ThoughtDAG 0.4.14-rc2.9、Protocol 0.3.3-rc2.1 和数据库 schema 24 不变。没有修改原生会话内容、Codex 镜像同步或引用版本固定规则。

验证：完整工作区构建通过。接入/运行证明、扩展接入、原生上下文服务及持久回执共 49 项测试通过；六个命名空间实际接入为 ready，未知插件版本仍拒绝。第一次运行有两项触发默认 5 秒超时，按项目既定 15 秒限时只重跑这两项后通过，没有修改业务断言。未知版本的拒绝用例使用固定未支持版本 `.999`，避免随当前版本升级而失去拒绝条件。
