# 接入兼容检查统一（2026-09-20）

## 范围与依据
用户确认修复 .35 已安装却无法注册的问题，采用统一检查、明确诊断，以及 adapter 拥有的协议与能力兼容判断，然后继续按标准教程部署。仍遵守新阻断即停止；不涉及 Codex。

## 改动
- 将两处分支版本名单合并为冻结的旧包兼容规则，至 .35；新版包按独立接入声明检查，不再每次补丁发布增加数组项。
- 新版 DSH 插件声明接入协议、宿主、会话格式和三项必要能力。未知/无效声明不能回退到旧包规则。
- 安装校验和注册共用包解析及兼容检查；保留实际 bundle、运行入口和宿主能力/回执检查，并记录实际插件 manifest 的校验值。
- 独立实例注册返回具体失败原因。引擎不新增宿主版本知识，DSH 规则仍在实例适配包。
- 候选 Engine 0.1.33-rc2.58 / 接入插件 0.2.26-rc2.36；Core .21、DAG .17 等未改版。

## 验证与现场
42 项聚焦测试通过，Contracts 构建、实例适配层及 Engine 类型检查、插件和发行包构建通过。实际安装 .36 后安装校验和注册成功，WebUI 与 DAG 保存/刷新成功，正常停止取得 closed 回执。

继续安装两侧 Bridge 后停在新的升级复核缺口：安装回执通过，但 check 不更新已有注册 fingerprint，注册仍为 needs-attention；标准教程未给出完成更新复核的 CLI 流程。按用户约定停止，不绕过。尚未验收 Bridge 运行、WebUI 绑定、贴纸和其余业务 adapters。详见本轮部署报告 compatibility-fix-and-bridge-blocker-20260920.md。
