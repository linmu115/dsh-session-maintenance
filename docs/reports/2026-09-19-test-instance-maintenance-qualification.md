# RC2 测试实例 Maintenance 接入补全

用户在同步页保存工作区后，设置页仍报告缺少实际宿主构件与 handle 能力回执。此前迁移已完成插件包安装，但新实例未完成宿主能力验证与启动绑定；同步选择只定义范围，不自动证明接入能力。

检查还发现测试实例使用的独立 RC2 宿主没有 GPT 插件需要的 5 种会话事件支持。基础 handle 测试先通过，并不能覆盖 GPT 原生事件。确认测试运行目录只属于该 Launcher 实例且实例已停止后，备份并将 13 个宿主包、202 个差异文件对齐到副本已使用的 GPT 兼容构件。源实例、Launcher 目录、目标其他插件不改。19 个插件、1,161 个插件文件及 63 个配套文件复核通过。

在独立、无凭据的 synthetic home 中运行实际测试 CLI 与模块，两进程 write / cold-read 验证普通持久化、并发写保护、撤销恢复、守卫释放、已安装插件网关，以及 5 种 GPT 事件保存和跨进程读取。验证通过后生成独立 Core/runtime 回执，经正式 integrations connect 接口接入。目标状态 connected，三项能力 supported，无接入问题；保存的同步策略与 profile 配置不变。

随后观察到真实实例由 Launcher 启动，托管 run running，冻结范围匹配已保存 revision 1：1 个工作区，不含未分组。旧浏览器凭据失效，经正式启动入口重新打开后，设置页“已接入”和同步页的 Launcher 名称、已保存范围均实测通过。观察到的浏览器警告来自扩展内容脚本，未观察到 Maintenance 页错误。未发送模型请求，未验证该真实 run 的正常停止。

用户追加反馈会话窗口为空。只读检查两个同名 DeepSeek 分类均无会话与子分类，本次 run 的 projection_sessions 为 0。用户确认此前已删除 DeepSeek 工作区，并明确不再处理；保留会话分类及同步范围，没有追加导入、迁移或重启。

部署工作区证据：`.artifacts/test-maintenance-qualification-20260919/qualification.json`、`connected.json`、`live-verified.json`、`workspace-diagnosis.json` 与相邻备份。实际绑定与运行检查不等于模型业务或全部插件 UI 验收。

开发历程草稿：由用户“不支持”反馈发现安装与接入验证脱节；基础能力通过后继续识别 GPT 宿主缺口，补齐真实构件后才出具回执。后续以运行范围与投影计数区分“已接入”和“已有会话可显示”，并按用户删除工作区后的最新要求结束分类排查。当前会话来源索引待绑定，本段不冒充正式可展开历史证据。
