# 学习交接跳过图片与分页历史适配

用户在任务 `01a0b8bb-ae8e-7202-af8d-ed8d46b04487` 反馈 `INTERNAL_ERROR: 学习消息包含非文本材料，不能静默过滤`，明确要求有图片就过滤掉。本次授权仅改变学习交接投影，不删除或改写源图片。

## 行为

- DSH `image` 与 Codex `input_image`/`output_image` 不进入交接正文，混合消息保留文字。纯图片消息使用统一 `[图片已跳过]` 占位，避免丢失提问而产生孤立回答。原图留在原平台历史；这不是识图或 OCR。
- 每条抽取消息记录可选 skippedImages，绑定/重新核对显示两端各自的跳过数量，同步/回收显示本次数量。数量只用于提示，不进入共同文字摘要。Codex 原始日志游标仍包含图片行，图片历史被改写仍会拒绝交接。
- 文件、音频、未知材料仍返回明确的 `LEARNING_UNSUPPORTED_CONTENT` / 409，不将用户授权扩大成丢弃全部非文本内容。现有工具执行、正文分歧、运行状态与事务保护保留。
- 只读检查“机试DeepLearning”时，DSH 原生消息含 2 张图片，Codex 同样抽取到 2 张图片；已证实图片会触发原错误。
- 同时发现该 Codex 任务是 `paginated`。旧适配器硬编码仅允许 `legacy`。依照本机 CLI 0.153.4 导出的协议和隔离实测，允许两种已验证格式；resume 使用正式 `excludeTurns: true`，继续通过原日志验证边界。未知格式仍拒绝。

## 验证和部署

适配器、Engine 闭环和页面相关测试最终覆盖 34 项，包含混合图文、纯图片、引用快照、原图保留、数量展示、原始游标保护及未知材料拒绝。Engine/依赖构建、Dashboard 构建与类型检查通过；发行接入 32 项和 portable 43 文件验证通过。

`scripts/accept-learning-codex.mjs legacy` 与 `... paginated` 均通过。本机真实 CLI 在临时 CODEX_HOME 中注入、关闭重开、继续下一轮，确认模型输入包含注入内容并可回收 2 条新问答。Responses 由本地固定回复服务代替，真实模型调用 0 次。

Engine **0.1.33-rc2.50** / Dashboard **0.1.11** 已备份后激活，接入 connected，DSH 保持停止。原 native 文件与 canonical heads 校验未变，现有配置及其他插件 1,165 个文件校验一致。34 个候选无运行状态阻塞，真实绑定仍为 0。

正式入口重新登录后，浏览器实际打开学习栏目，已核验图片过滤与纯图片占位说明可见。真实绑定成功后的计数展示仅组件测试通过；真实绑定及同步/回收尚未验收。

安装/备份证据：`D:/AI/DeepSeekHarness-Plugin/artifacts/learning-images-20260919`。正式包：`.artifacts/learning-images-20260919/dsh-session-maintenance-engine-0.1.33-rc2.50.tgz`，SHA-256 `0eddd1b5284faab895695e0595887e3e2ae45a8a3062765db7758c6a214f61d5`。

## 尚未通过的关联边界

“机试DeepLearning”的只读原生历史预检得到 DSH 49 条、Codex 54 条学习消息；过滤图片后仍不构成当前要求的共同文字前缀。该预检不写绑定或正文，不能把图片过滤成功写成真实关联成功，也不能推断仅因数量差异就可合并。本次没有放松共同历史校验、强制关联或同步真实内容。当前学习解析与普通导入对压缩历史、系统附加说明的语义差异需继续独立核对；未宣称所有差异都是用户正文冲突。

过程草稿：定位非文本拒绝 → 核实图片类型和数量 → 两端过滤与占位/计数回归 → 发现分页历史门槛 → 真实 CLI 隔离验收两种模式 → 只读核对剩余前缀问题 → 备份升级。未将此草稿冒充已索引的历史事件。
