# 重启重复会话、归档与旧派生标题修复

## 用户要求与验收

2026-09-17 用户报告官方归档后重启再次出现“机试DeepLearning”，点击后标题变成工作目录名，并明确要求执行修复。范围为准备事件恢复、归档投影和现存旧派生标题；不删除历史或按同名合并正文。

验收：纯准备尾部不创建派生历史；含真实事件的混合尾部完整保留；已归档 canonical 会话经官方宿主归档 API 隐藏，缺失标记不撤销宿主归档；旧派生会话通过官方重命名补写标题且原消息序号不变。现存重复项需先验证差异只有准备事件，再归档而非删除。部署保留回退包和数据库备份，重启后复核。

## 诊断与开发历程

本任务可见用户消息与只读工具检查为来源，详细事件索引已绑定到[正式开发历程](../project/records/history/recovery-archive-title.md)，仅保存所选任务的公开事件定位与指纹。真实数据库显示旧归档仍存在，2026-09-16 的恢复操作额外创建两个派生 ID；其中一个 canonical 已归档但宿主未归档。原始镜像已有标题事件，三个旧派生文件没有标题事件。

恢复 WAL 的既有过滤没有覆盖新扫描的原生尾部。Adapter 的目录 payload 未携带 archivedAt，宿主启动无法恢复这项状态。旧派生序号不可挪动，因此标题采用官方尾部追加修复，不重排消息。

## 实施与验证

Adapter / GPT 扩展 / projection lifecycle / 宿主归档与目录测试 26 文件、140 项通过；随后补充归档字段和完整准备尾部恢复测试，针对的 15 项通过（包含上述既有测试）。Adapter 与插件类型检查通过，完整 workspace 构建通过。构建时暴露 GPT 扩展导出推断依赖私有路径的问题，使用宿主 Adapter 的显式公开类型修复，运行行为不变。

候选版本 Engine 0.1.33-rc2.31、插件 0.2.26-rc2.25、Adapter 0.1.4。构件与真实运行验收结果在完成后补充；本段不表示已经部署。

## 部署、数据修复与重启验收

已部署到 Launcher `0.1.5-rc.2 副本 / web`，Engine 0.1.33-rc2.31、插件 0.2.26-rc2.25、Adapter 0.1.4。源码修复提交 dec850f；发布兼容清单补齐提交 026a749。安装后首次连接因显式版本清单未登记新版本被拒绝，补齐并通过 31 项接入测试及 1 项回执测试后，重新打包、安装与验证成功；没有绕过回执。

相关产品回归共 174 项通过（26 文件原 140 项，补充 2 项，接入与回执 32 项），完整构建和受影响类型检查通过。初次发布包三份输出可重复；验证脚本不支持传入输出参数，最终路径检查失败后改为显式比较三份文件与内嵌插件，均相等。兼容清单补齐后的最终包重新核验内嵌插件匹配。

旧引擎最后一次正常收尾又生成一份纯准备事件的派生项，因此实际核对与处理四份。四份 inherited prefix 与各自固定 base version 逐条相同，新增事件只有准备配置；通过官方会话接口归档，没有删除历史。对三份缺失标题的旧派生会话执行官方 rename，追加 session/title 而不移动消息。官方 rename 响应先于物理文件刷盘，初次即时检查没有看到新事件；等待刷盘并按已保存意图续跑后，原生文件、canonical 和官方列表一致。

第二次正常重启验收运行 `run-6b27611d-43ba-40b0-8bd0-45dc1c2f800b`：

- 重启新增会话身份 0；唯一可见的 DeepLearning 条目是原始“机试DeepLearning”镜像。
- 四份修复派生项在 canonical 与官方宿主均保持归档，原生日志标题正确，原事件前缀完整保留。
- 升级前 252,172 条 canonical event_json 逐条不变；1072 项其它插件及主实例文件指纹不变。
- 7 个业务命名空间 ready，数据库 quick_check=ok；未调用模型。

本机备份、修复计划、官方操作结果和验证回执在 `D:/AI/DeepSeekHarness-Plugin/artifacts/recovery-archive-title-20260917/`。`verification.json` 为最终验收，`before/` 保留原数据库、配置和目标原生文件；这些私有材料不提交源码。验证覆盖实际接口和文件冷读取，未声称截图或手动 UI 验收。

最终 Engine 包 SHA-256：4121fdbbb7adcdbf570cb84d61decb5fda3109036e7d578c4c8d9c65c8703162；插件包 SHA-256：92c53cb066b1d2b2236b01d358b21f2954d4d65bc0f7f9ce85688b5286699c4d。
