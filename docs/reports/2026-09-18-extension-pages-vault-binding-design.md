# 公开扩展页与 Vault 绑定协作设计变更报告

日期：2026-09-18。变更类型：文档与项目地图。

## 修改范围

- 新增公开业务页面／栏目贡献、参与名单汇总、实例有效范围及独立运行的设计与验收。
- 将 Obsidian 的 Bridge 绑定作为外部提供方接口，保存本项目的消费责任及数据归属。
- 更新 Dashboard、宿主、业务 Adapter、扩展服务和 Obsidian 接入入口，绑定需求与公开来源历程。
- 不新增 Harness Adapter，不改变既有领域写入规则或当前部署。

## 验证结果

- 项目地图校验通过：72 条记录，错误与警告均为 0；新增需求、接口草案和图节点关联均可解析。
- 开发历程索引校验通过：10 项任务、10 份按需来源资产；本次只索引已说明范围内的公开事件。
- 两项目本次变更的 25 份 Markdown 共检查 141 个相对链接，全部目标存在；Suite 一处跨项目链接修正后复查通过。
- 已核对双方稳定项目 ID 及 3 个新增合同的存在性：Bridge 绑定、公开扩展页、实例有效范围。
- 本项目文档差异格式检查通过。已有产品源文件的其他在途改动不属于本次文档任务，不纳入本次提交或验证结论。

## 验证边界

遵循本仓文档任务的针对性检查，本次运行地图、来源索引、链接和差异校验，不运行无改动产品的整套测试。未修改产品源代码、配置或数据库，没有安装或部署。实际同步范围入口和跨进程视图装载仍是已记录的实施核查项。
